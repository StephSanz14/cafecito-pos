//SETUP INICIAL//
Para trabajar en Frontend instalamos Tailwind
En la estructura de mis carpetas, construí una que se llama layout en donde cree dos componentes
el primero es el shell el cual es como un contenedor o un armazon y a partir de ahi van a estar 
intercambiandose las vistas entre ventas, productos y clientes
también cree otro componente que es el sidebar, esta barra de navegación me da la opcion de 
rutear hacia que componente me voy a dirigir
Posteriormente cree una carpeta que la llame pages, en ella cree tres componentes: ventas, clientes y productos
para las vistas y otras dos que son para logearse y para registrarse
Esas fueron mis configuraciones iniciales...

1️⃣ Rutas + Shell ✔️
2️⃣ Login / Register ✔️
3️⃣ TokenService ✔️
4️⃣ AuthGuard ← aquí estamos
5️⃣ RoleGuard
6️⃣ Interceptor
7️⃣ Services de negocio
8️⃣ Refresh token (opcional)

//RUTAS GLOBALES DE VISTAS//
Para poderlas ver en el navegador, me fui al archivo de app.routes.ts
ahi configure las rutas con esta estructura
 //Login Route
    {
    path: 'login',
    loadComponent: () =>
      import('../pages/login/login.component').then((c) => c.LoginComponent),
    title: 'Login',
  },
y para camiar entre vistas use rutas children:
Las rutas con children sirven para:
Compartir un layout (estructura base) y cambiar solo el contenido interno
Piensa en esto:
Un ShellComponent con:
sidebar
header
footer
Y dentro de él, distintas páginas:
ventas
clientes
productos
👉 El shell no cambia, solo cambia lo que va dentro.

//injectable
@Injectable() es un decorador que le dice a Angular:

“Esta clase puede ser inyectada como dependencia en otros componentes, servicios o guards”.
¿Qué pasa si NO pongo @Injectable?
Angular NO puede inyectarlo en otros lados.
Esto fallará ❌:
constructor(private authService: AuthService) {}

//funcion asyncrona
Una función es asíncrona cuando NO puede devolver el resultado de inmediato y tiene que esperar algo externo o lento.
👉 En JavaScript eso significa: devuelve una Promesa.
¿Cuándo una función debe ser asíncrona (async)?

Una función es asíncrona cuando NO puede devolver el resultado de inmediato y tiene que esperar algo externo o lento.

👉 En JavaScript eso significa: devuelve una Promesa.

Casos típicos (los importantes)
1️⃣ Conectarse con backend ✅
✔️ Peticiones HTTP
fetch
HttpClient en Angular
axios
async getSales() {
  return await this.http.get('/api/sales');
}

2️⃣ Subir o descargar archivos ✅
✔️ Archivos grandes
✔️ Lectura de archivos
async uploadFile(file: File) {
  await this.http.post('/upload', file);
}

3️⃣ Acceso a base de datos ✅
(en backend)
const sale = await Sale.findById(id);
MongoDB
MySQL
PostgreSQL
Firebase

4️⃣ Lectura o escritura de archivos del sistema ✅
(Node.js)
await fs.promises.readFile('file.txt');

5️⃣ Autenticación / seguridad ✅
✔️ Hash de contraseñas
✔️ Generar tokens
const hash = await bcrypt.hash(password, 10);

6️⃣ Llamar APIs externas ✅
✔️ Google Maps
✔️ Stripe
✔️ PayPal
await stripe.paymentIntents.create(...);

7️⃣ Temporizadores o esperas reales ✅
await new Promise(resolve => setTimeout(resolve, 2000));

1️⃣ ¿Qué es HttpClient?

Piensa en HttpClient como el mensajero de Angular.
👉 Su único trabajo es:
mandar requests HTTP (GET, POST, PUT, DELETE)
recibir la respuesta del backend
Ejemplo mental:
“Angular, ve al backend y pregúntale algo”
Eso lo hace HttpClient.
¿Por qué HttpClient devuelve un Observable?

Aquí está la parte clave 👇

Angular NO devuelve datos inmediatamente

Porque:
el backend puede tardar
puede fallar
puede responder después
Entonces Angular dice:
“No te doy el dato aún.
Te doy algo que me avisa cuando llegue.”
Ese “algo” es un Observable.

3️⃣ ¿Qué es un Observable? (en español real)
Un Observable es:

“una promesa de que en el futuro llegará un valor (o un error)”.

No es el valor.
Es la espera.

Ejemplo de la vida real:
Pediste un café ☕
No lo tienes aún
Te dan un ticket y te llaman cuando esté listo
📌 El ticket es el Observable
📌 El café es el valor

-----GUARDS----

¿Dónde actúan? En la navegación de rutas del frontend.
¿Qué hacen?
Antes de entrar a una ruta, revisan condiciones:
¿hay token? (authGuard)
¿tiene rol admin? (roleGuard)

Si no cumple:
lo mandan a /login o a /ventas
¿Para qué te sirve AHORITA?
✅ Para que el usuario no vea páginas internas si no está logueado.
✅ Para que un seller no entre a cosas que solo admin puede.

Ejemplo mental:
“El guard es el guardia de seguridad en la puerta del edificio. Si no tienes credencial, no entras.”

---Interceptor (HTTP)---

¿Dónde actúa? En todas las llamadas HTTP (GET/POST/PUT/DELETE).
¿Qué hace?
Agarra tu token del TokenService
Lo pega automáticamente en cada request:
Authorization: Bearer <token>
(Opcional después) Si el backend responde 401, intenta refresh y reintenta
¿Para qué te sirve AHORITA?
✅ Para que tus endpoints privados (/customers, /sales, CRUD products) funcionen sin que estés copiando el header en cada request.
Ejemplo mental:
“Cada vez que sales de tu casa, el interceptor se asegura de que lleves tu INE en la cartera.”